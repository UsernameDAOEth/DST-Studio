kind = "web"
previewPath = "/"
title = "DST — Deterministic Signal Trading"
version = "1.0.0"
id = "artifacts/dst"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19979

[services.development]
run = "pnpm --filter @workspace/dst run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dst", "run", "build" ]
publicDir = "artifacts/dst/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19979"
BASE_PATH = "/"
